# Create a script or configure an existing script in your OS to launch programs at system startup (Add the autosave.sh script to the directory with the program autostart script and select the directory root and the autostart itself, for example, in opt and the autostart script and autosave.sh: /opt/autosave.sh and place it at the beginning of the autostart script above all autostarts)
